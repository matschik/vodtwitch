#!/usr/bin/env node

const [vodIdOrUrl] = process.argv.slice(2);

const downloadTwitchVod = require("../");

async function main() {
  await downloadTwitchVod(vodIdOrUrl, { log: true });
}

main().catch((err) => console.error(err));
